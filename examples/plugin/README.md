# Pulse expert pack example

This directory demonstrates how standalone project Skills and agents become a shareable, namespaced plugin.

```bash
claude --plugin-dir ./examples/plugin
```

Then run `/pulse-expert-pack:quality-gate` and confirm that `release-reviewer` appears in `/agents`.
